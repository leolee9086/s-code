- 适用于任何规模代码库的快速内容搜索工具
- 使用正则表达式搜索文件内容
- 支持完整的正则表达式语法（例如 "log.*Error"、"function\s+\w+" 等）
- 通过 include 参数按文件模式过滤（例如 "*.js"、"*.{ts,tsx}"）
- 返回至少有一个匹配项的文件路径和行号，按修改时间排序
- 当你需要查找包含特定模式的文件时使用此工具
- 如果需要识别/统计文件中的匹配数量，直接使用 Bash 工具配合 `rg`（ripgrep）。不要使用 `grep`
- 当进行可能需要多轮 glob 和 grep 的开放式搜索时，使用 Task 工具