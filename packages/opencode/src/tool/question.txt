当你需要在执行过程中向用户提问时使用此工具。它可以让你：
1. 收集用户偏好或需求
2. 澄清模糊的指令
3. 在工作中获取实现选择的决策
4. 为用户提供方向选择

使用说明：
- 当启用 `custom`（默认）时，会自动添加"输入你自己的答案"选项；不要包含"其他"或任何兜底选项
- 答案以标签数组形式返回；设置 `multiple: true` 以允许多选
- 如果你推荐特定选项，将其设为列表中的第一个选项，并在标签末尾添加"（推荐）"