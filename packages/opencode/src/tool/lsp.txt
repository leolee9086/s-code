与语言服务器协议（LSP）服务器交互，获取代码智能功能。

支持的操作：
- goToDefinition：查找符号定义的位置
- findReferences：查找符号的所有引用位置
- hover：获取符号的悬停信息（文档、类型信息）
- documentSymbol：获取文档中的所有符号（函数、类、变量）
- workspaceSymbol：列出匹配查询字符串的整个项目范围的符号
- goToImplementation：查找接口或抽象方法的实现
- prepareCallHierarchy：获取位置处的调用层次结构项（函数/方法）
- incomingCalls：查找所有调用该位置处函数的函数/方法
- outgoingCalls：查找该位置处函数调用的所有函数/方法

所有操作都需要：
- filePath：要操作的文件
- line：行号（从 1 开始，与编辑器中显示一致）
- character：字符偏移（从 1 开始，与编辑器中显示一致）

workspaceSymbol 还接受：
- query：用于过滤符号的查询字符串。空字符串请求所有符号。

对于 workspaceSymbol，filePath 不会在 LSP workspace/symbol 请求中发送。opencode 使用它来选择并启动匹配的 LSP 服务器。

注意：LSP 服务器必须为文件类型配置。如果没有可用的服务器，将返回错误。