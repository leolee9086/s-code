将文件写入本地文件系统。

使用说明：
- 如果提供的路径已存在文件，此工具将覆盖它。
- 如果是已有文件，你必须先使用 Read 工具读取文件内容。如果未先读取文件，此工具将报错。
- 始终优先编辑代码库中已有的文件。除非明确需要，否则切勿创建新文件。
- 切勿主动创建文档文件（*.md）或 README 文件。仅在用户明确请求时才创建文档文件。
- 仅在用户明确要求时使用 emoji。避免在文件中写入 emoji，除非被要求。